package Inheritance;

public interface interface_one {
   // in the interface abstract methods which means 
	// if the methods doesn't have the body then it is know as abstract
	public void name_1();
	
}
