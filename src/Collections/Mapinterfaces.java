package Collections;
import java.util.*;

// Generally the maps is the collection of key-values-pair but set is the collection of keys only
public class Mapinterfaces {
    public static void main(String[] args){
//        Map<Integer,String> list = new TreeMap<>(); // treemap the sort the data according ot the keys
//        list.put(100,"prakash");
//        list.put(200,"prashanth");
//        list.put(150,"venkatesh");
//        System.out.println(list);

        // FUNCTIONS IN MAP<k,v>
   // ->put(k key, v value) ->add the given association
   // ->get(object key) -> returns the associated value or null
   // ->boolean isEmpty() -> return true if map is empyt
   //->remove(object key) -> removes the mapping and return value or null...
   // -> boolean containsKey(Object key) -? return true if key is present..
   // -> boolean containsValue(object val) ->return true if the value is present
   // -> V replace(K key, V value) -> add new association only if key is present already
   // -> int size() // returns size of map

   // Set<K> keySet() -> return a set view of the keys..
   // Collection<V> values() -> return a collection of the values
   // Set<Map.Entry<k,v>> entrySet() -> return a set view of the mapping
        // in above case view means if you are make any changes in the set it will reflect to the mapand vice varsa
   // v getOrDefault(Object key, V default) -> return existing values or default
    // void clear() // remove all mappings..

//        HashMap<String,Integer> list = new HashMap<>();
//        list.put("prakash",10);
//        list.put("prashanth",15);
//        list.put("venkatesh",20);
//        System.out.println(list);
//        System.out.println(list.size());
//        for(Map.Entry<String,Integer> e : list.entrySet())
//            System.out.println(e.getKey()+" "+e.getValue());
//        if(list.containsKey("prakash"))
//            System.out.println("yes it's there");
//        else
//            System.out.println("No it's not there");
//
//        list.remove("prakash");
//        System.out.println(list);
//
//        if(list.containsKey("prakash"))
//            System.out.println("yes it's there");
//        else
//            System.out.println("No it's not there");
//
//        System.out.println(list.size());
//
//        if(list.containsValue(10))
//            System.out.println("yes it's there");
//        else
//            System.out.println("No it's not there");
//
//        System.out.println(list.get("prakash"));
//        System.out.println(list.get("prashanth"));

    }
}
